name: Discovery Service

on:
  push:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'corretto'

      - name: Create application.yml
        run: |
          mkdir -p ./discovery-server/src/main/resources
          echo "${{ secrets.APPLICATION_ORDR }}" | base64 --decode > ./discovery-server/src/main/resources/application.yml

      - name: Build with Gradle
        run: |
          cd discovery-server
          ./gradlew build -x test

      - name: Docker build
        run: |
          cd discovery-server
          docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
          docker build -t app .
          docker tag app ${{ secrets.DOCKER_USERNAME }}/discovery-server:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/discovery-server:latest

      - name: Deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_SSH_KEY_ORDR }} # EC2 인스턴스 퍼블릭 DNS
          username: ${{ secrets.USERNAME }} # EC2 유저내임
          key: ${{ secrets.PRIVATE_KEY }} # pem 키
          script: |
            docker pull ${{ secrets.DOCKER_USERNAME }}/discovery-server:latest
            docker stop $(docker ps -a -q)
            docker run -d --log-driver=syslog -p 8761:8761 ${{ secrets.DOCKER_USERNAME }}/discovery-server:latest
            docker rm $(docker ps --filter 'status=exited' -a -q)
            docker image prune -a -f